node_modules/
dist/
build/
.env
.env.*
!.env.example
*.log
.DS_Store
coverage/
.vite/
apps/web/dist/
var/
storage/
playwright-report/
test-results/
.browser-profiles/
tsconfig.tsbuildinfo
*.tsbuildinfo
test-results/
playwright-report/
var/

# twscrape writes this into the working directory. It was tracked and shipped
# empty, which is harmless right up until somebody adds an account to it and
# commits their credentials without noticing.
accounts.db
*.db-journal

# Editor and assistant tooling, per-developer rather than per-project.
.claude/
.cursor/
.aider*
.vscode/
.idea/

# Repository backups. Never publishable: a bundle is the whole history.
*.bundle

# Windows packaging output. The staged app and the installer are build
# products; the release workflow produces them from source.
build/windows/
